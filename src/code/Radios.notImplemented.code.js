import React from 'react';
import {render} from 'react-dom';

// start
const Radios = () => {
  return <p>TODO...</p>;
};

render(
  <Radios
    name="something"
    options={[{value: 'a', label: 'A'}, {value: 'b', label: 'B'}]}
  />
);
